generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("postgresql://prescriptions_api_user:dr8FeTB4dJIryDSfMkOQ34V7cCssLRGJ@dpg-crpnic3v2p9s7388lmrg-a.oregon-postgres.render.com/prescriptions_api")
}

model User {
  id       String @id @default(uuid())
  name     String
  email    String @unique
  password String

  @@map("users")
}

model Prescription {
  id            String   @id @default(uuid())
  medicalRecord String
  name          String
  medicine      String
  unit          String
  dose          Float
  via           String
  posology      String
  posologyDays  String[]

  @@map("prescriptions")
}
