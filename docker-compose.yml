services:
  prescriptions-api-postgres:
    container_name: prescriptions-api-postgres
    image: bitnami/postgresql
    ports:
      - 5433:5432
    environment:
      POSTGRES_USER: monitora-atb
      POSTGRES_PASSWORD: monitora-atb
      POSTGRES_DB: monitora-atb-db
